<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:LogAnalyzerForWindows.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1000" d:DesignHeight="700"
        x:Class="LogAnalyzerForWindows.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/icon.ico"
        Opacity="0.9"
        Title="Log Analyzer For Windows">

    <Design.DataContext>
        <vm:MainWindowViewModel />
    </Design.DataContext>

    <Window.Styles>
        <StyleInclude Source="/Styles/Styles.axaml" />
        <StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml"/>
    </Window.Styles>

    <Grid RowDefinitions="Auto, *, Auto, Auto, Auto">
        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="10,5">
            <ToggleButton Content="Database Mode"
                          IsChecked="{Binding UseDatabaseMode}"
                          IsEnabled="{Binding CanToggleDatabaseMode}">
                <ToolTip.Tip>View historical logs from database</ToolTip.Tip>
            </ToggleButton>
            <Button Content="Clear All History" Command="{Binding ClearHistoryCommand}">
                <ToolTip.Tip>Delete all logs from database</ToolTip.Tip>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1" ColumnDefinitions="Auto, *">
            <!-- Left Panel - Filters and Controls -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Margin="5,20,5,0">
                <StackPanel Margin="10" Spacing="10" MinWidth="250">
                    <TextBlock Text="Filters" FontWeight="Bold" FontSize="14" />

                    <!-- Log Source Selection -->
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <Label Content="Source:" VerticalAlignment="Center" MinWidth="60" />
                        <ComboBox x:Name="LogSourceList" MinWidth="160"
                                  ItemsSource="{Binding LogSources}"
                                  SelectedItem="{Binding SelectedLogSource}">
                            <ToolTip.Tip>Select Windows Event Log source (System, Application, Security, etc.)</ToolTip.Tip>
                        </ComboBox>
                    </StackPanel>

                    <!-- Log Level Selection -->
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <Label Content="Level:" VerticalAlignment="Center" MinWidth="60" />
                        <ComboBox x:Name="LogLevelList" MinWidth="160"
                                  ItemsSource="{Binding LogLevels}"
                                  SelectedItem="{Binding SelectedLogLevel}">
                            <ToolTip.Tip>Available log levels for the selected source</ToolTip.Tip>
                        </ComboBox>
                    </StackPanel>

                    <!-- Time Range Selection -->
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <Label Content="Time:" VerticalAlignment="Center" MinWidth="60" />
                        <ComboBox x:Name="TimeList" MinWidth="160"
                                  ItemsSource="{Binding Times}"
                                  SelectedItem="{Binding SelectedTime}">
                            <ToolTip.Tip>Time range to monitor</ToolTip.Tip>
                        </ComboBox>
                    </StackPanel>

                    <Separator Margin="0,10,0,10" />

                    <TextBlock Text="Output" FontWeight="Bold" FontSize="14" />

                    <!-- Format Selection -->
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <Label Content="Format:" VerticalAlignment="Center" MinWidth="60" />
                        <ComboBox x:Name="FormatComboBox" MinWidth="160"
                                  ItemsSource="{Binding Formats}"
                                  SelectedItem="{Binding SelectedFormat}">
                            <ToolTip.Tip>Export format (txt or json)</ToolTip.Tip>
                        </ComboBox>
                    </StackPanel>

                    <!-- Save Logs Button -->
                    <Button Command="{Binding SaveCommand}" IsEnabled="{Binding CanSave}" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Image Source="{SvgImage /Assets/save.svg}" Height="16" Width="16" />
                            <TextBlock Text="Save Logs" VerticalAlignment="Center" />
                        </StackPanel>
                        <ToolTip.Tip>Saves the current logs to a file</ToolTip.Tip>
                    </Button>

                    <!-- Open Folder Button -->
                    <Button Command="{Binding OpenFolderCommand}" IsEnabled="{Binding IsFolderExists}" Margin="0,5,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Image Source="{SvgImage /Assets/folder-plus.svg}" Height="16" Width="16" />
                            <TextBlock Text="Open Logs Folder" VerticalAlignment="Center" />
                        </StackPanel>
                        <ToolTip.Tip>Opens the LogAnalyzerForWindows directory</ToolTip.Tip>
                    </Button>

                    <!-- Archive Button -->
                    <Button Command="{Binding ArchiveLatestFolderCommand}" IsEnabled="{Binding IsFolderExists}"
                            Margin="0,5,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Image Source="{SvgImage /Assets/archive.svg}" Height="16" Width="16" />
                            <TextBlock Text="Archive Latest Logs" VerticalAlignment="Center" />
                        </StackPanel>
                        <ToolTip.Tip>Archives the latest daily log folder</ToolTip.Tip>
                    </Button>

                    <Separator Margin="0,10,0,10" />

                    <TextBlock Text="Email" FontWeight="Bold" FontSize="14" />

                    <!-- Email Input -->
                    <StackPanel Orientation="Horizontal" Spacing="5">
                        <Label Content="Email:" VerticalAlignment="Center" MinWidth="60" />
                        <TextBox x:Name="EmailInput" Text="{Binding UserEmail}" MinWidth="160"
                                 Watermark="email@example.com">
                            <ToolTip.Tip>Enter recipient email address</ToolTip.Tip>
                        </TextBox>
                    </StackPanel>

                    <!-- Send Email Button -->
                    <Button Command="{Binding SendEmailCommand}" Margin="0,10,0,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Image Source="{SvgImage /Assets/mail.svg}" Height="16" Width="16" />
                            <TextBlock Text="Send Latest Archive" VerticalAlignment="Center" />
                        </StackPanel>
                        <ToolTip.Tip>Sends the latest zip file to the specified email</ToolTip.Tip>
                    </Button>
                </StackPanel>
            </ScrollViewer>

            <!-- Right Panel - Content Area -->
            <Grid Grid.Column="1" Margin="5,30,10,10">
                <!-- Real-time monitoring output -->
                <TextBox x:Name="Output"
                         IsVisible="{Binding !UseDatabaseMode}"
                         Text="{Binding OutputText}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         FontFamily="Consolas, Courier New, Monospace"
                         FontSize="12"
                         Background="#1E1E1E"
                         Foreground="#D4D4D4">
                    <ToolTip.Tip>Real-time log monitoring output</ToolTip.Tip>
                </TextBox>

                <!-- Database view with pagination -->
                <Grid IsVisible="{Binding UseDatabaseMode}" RowDefinitions="Auto, *, Auto">
                    <!-- Session Filter -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="10,5">
                        <TextBlock Text="Session:" VerticalAlignment="Center" FontWeight="Bold" />
                        <ComboBox ItemsSource="{Binding AvailableSessions}"
                                  SelectedItem="{Binding SelectedSession}"
                                  MinWidth="300"
                                  ToolTip.Tip="Filter logs by monitoring session" />
                    </StackPanel>

                    <!-- Data Grid -->
                    <ScrollViewer Grid.Row="1"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto"
                                  Margin="0,10,0,0">
                        <DataGrid ItemsSource="{Binding PaginationViewModel.CurrentPageLogs}"
                                  IsReadOnly="True"
                                  AutoGenerateColumns="False"
                                  GridLinesVisibility="All"
                                  BorderThickness="1"
                                  BorderBrush="#CCCCCC"
                                  CanUserReorderColumns="True"
                                  CanUserResizeColumns="True"
                                  CanUserSortColumns="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Timestamp"
                                                    Binding="{Binding Timestamp, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}"
                                                    Width="180"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="Level"
                                                    Binding="{Binding Level}"
                                                    Width="120"
                                                    IsReadOnly="True" />
                                <DataGridTextColumn Header="Message"
                                                    Binding="{Binding Message}"
                                                    Width="*"
                                                    MinWidth="400"
                                                    IsReadOnly="True" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>

                    <!-- Pagination Controls -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="10"
                                Margin="0,10">
                        <Button Content="⏮ First"
                                Command="{Binding PaginationViewModel.FirstPageCommand}"
                                Width="80"
                                ToolTip.Tip="Go to first page" />

                        <Button Content="◀ Prev"
                                Command="{Binding PaginationViewModel.PreviousPageCommand}"
                                Width="80"
                                ToolTip.Tip="Go to previous page" />

                        <TextBlock Text="{Binding PaginationViewModel.PageInfo}"
                                   VerticalAlignment="Center"
                                   FontWeight="Bold"
                                   Margin="10,0"
                                   MinWidth="100"
                                   TextAlignment="Center" />

                        <Button Content="Next ▶"
                                Command="{Binding PaginationViewModel.NextPageCommand}"
                                Width="80"
                                ToolTip.Tip="Go to next page" />

                        <Button Content="Last ⏭"
                                Command="{Binding PaginationViewModel.LastPageCommand}"
                                Width="80"
                                ToolTip.Tip="Go to last page" />

                        <ComboBox ItemsSource="{Binding PaginationViewModel.PageSizes}"
                                  SelectedItem="{Binding PaginationViewModel.PageSize}"
                                  Width="80"
                                  ToolTip.Tip="Items per page" />

                        <Button Content="🔄"
                                Command="{Binding PaginationViewModel.RefreshCommand}"
                                Width="40"
                                ToolTip.Tip="Refresh data" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#333333" Padding="10" Margin="10,5,10,5" CornerRadius="3">
            <TextBlock Foreground="White"
                       FontSize="12"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Text="{Binding TextBlock}"
                       TextWrapping="Wrap" />
        </Border>

        <!-- Control Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Spacing="20"
                    Margin="10">
            <Button Command="{Binding StartCommand}" MinWidth="100" Height="40">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Image Source="{SvgImage /Assets/play.svg}" Height="16" Width="16" />
                    <TextBlock Text="Start" VerticalAlignment="Center" />
                </StackPanel>
                <ToolTip.Tip>Starts monitoring logs</ToolTip.Tip>
            </Button>

            <Button Command="{Binding StopCommand}" MinWidth="100" Height="40">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Image Source="{SvgImage /Assets/pause.svg}" Height="16" Width="16" />
                    <TextBlock Text="Stop" VerticalAlignment="Center" />
                </StackPanel>
                <ToolTip.Tip>Stops monitoring logs</ToolTip.Tip>
            </Button>
        </StackPanel>

        <!-- Loading Indicator -->
        <Border Grid.Row="0" Grid.RowSpan="5"
                IsVisible="{Binding IsLoading}"
                Background="#80000000"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ZIndex="1000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
                <TextBlock Text="Loading..."
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center" />
                <ProgressBar IsIndeterminate="True"
                             Width="200"
                             Height="6" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
